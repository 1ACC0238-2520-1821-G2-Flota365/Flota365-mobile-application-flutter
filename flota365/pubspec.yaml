name: flota365
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  # --- HIVE (Storage local) ---
  hive: ^2.2.3
  hive_flutter: ^1.1.0

  # --- PAQUETES DE RED Y ESTADO ---
  dio: ^5.7.0
  flutter_secure_storage: ^9.0.0
  flutter_bloc: ^9.1.1
  equatable: ^2.0.5
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
